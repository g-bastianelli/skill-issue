# Test project

A minimal CLAUDE.md fixture used together with `stack-contradiction.md`.

## Stack rules

- **No new npm/bun deps** in any plugin. Stick to the standard library
  (`node:fs`, `node:path`, etc.) unless explicitly approved.
